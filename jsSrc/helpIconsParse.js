/*
import {icons} from "shelter/data/helpIcons";

console.log(icons);

const container = document.getElementById('help__icons-container');
const figureClass = 'help__icon';
const imgClass = 'help__img';
const figcaptionClass = 'help__icon-caption';

export async function init() {
    for (const icon of icons) {
        const icon = document.createElement('figure');
        icon.classList.add(figureClass);
        icon.imgSrc.classList.add(imgClass);
        icon.appendChild(icon.imgSrc)
        const figcaption = document.createElement('figcaption');
        figcaption.innerHTML = icon.name;
        figcaption.classList.add(figcaptionClass);
        icon.appendChild(figcaption);
        container.appendChild(icon);
    }
}*/
